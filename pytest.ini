[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    # Aviso interno de chromadb al leer la configuración que guarda langchain-chroma.
    ignore:legacy embedding function config:DeprecationWarning
