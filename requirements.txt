# Dependencias que pide la consigna
openai>=3.11.0
anthropic>=1.4.0
pydantic>=2.13.0
python-dotenv>=1.2.0

# Desarrollo (para correr los tests, no hacen falta para usar el cliente)
pytest>=8.0.0
pytest-asyncio>=0.24.0
